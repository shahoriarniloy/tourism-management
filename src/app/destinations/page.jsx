import Destinations from '@/components/Destinations';
import React from 'react';

const page = () => {
    return (
        <div>
            <Destinations />
        </div>
    );
};

export default page;